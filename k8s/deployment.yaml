apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: identity-service
  name: identity-service
  namespace: default
spec:
  replicas: 1 #CHANGE
  minReadySeconds: 45
  selector:
    matchLabels:
      app: identity-service
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: identity-service
    spec:
      containers:
      - image: vebstechbee03/tech-bee:identity-service-latest
        imagePullPolicy: Always
        name: identity-service
        ports:
        - name: liveness-port
          containerPort: 8085
        resources: #CHANGE
          requests:
            cpu: 100m
            memory: 512Mi
          limits:
            cpu: 500m
            memory: 1024Mi #256Mi 
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: liveness-port
          failureThreshold: 5
          periodSeconds: 10
          initialDelaySeconds: 60
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: liveness-port
          failureThreshold: 5
          periodSeconds: 10
          initialDelaySeconds: 60
        env:
          - name: SPRING_PROFILES_ACTIVE
            value: uat
      restartPolicy: Always
      terminationGracePeriodSeconds: 30